#A This function should be modified with what the user wants to do previous to
# the compilation.
# For instance, untar the code.

## directory/to/be/created
mkdir -p /var/sds/connectors/streaming/stratio-connector-streaming-store
mkdir -p /var/log/sds/connectors/streaming
mkdir -p /opt/sds/connectors/streaming
